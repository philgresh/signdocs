/* eslint-disable react/prop-types */
import React from 'react';

const DocUpload = () => (
  <div>
    <h2>You&apos;ve created a document! </h2>
    <br />
    <p>
      Next, prepare the document for signatures by adding some content fields,
      like signature boxes or text fields.
    </p>
    <p>
      Select one of your signatories on the right, and then drag a content field
      to the PDF which will be assigned to them.
    </p>
  </div>
);

export default DocUpload;
